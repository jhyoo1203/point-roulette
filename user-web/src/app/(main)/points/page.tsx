import { PointsPageContent } from '@/features/points/components/PointsPageContent';

export const metadata = {
  title: '내 포인트 - Point Roulette',
};

export default function PointsPage() {
  return <PointsPageContent />;
}
